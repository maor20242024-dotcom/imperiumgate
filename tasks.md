Khalid537

Copy

Delete

🎯 الخلاصة أولًا

لا، أنت لا تحتاج إلى Google Maps API إطلاقًا

(ما دمت تُخزّن الإحداثيات والبيانات المكانية داخل ملفات JSON بنفسك).

⸻

🧭 لماذا وُجد Google Maps API أصلًا؟

Google Maps API عادةً يُستخدم لأحد هذه الأسباب:

1.	عرض خريطة تفاعلية حقيقية (zoom, pan, satellite, traffic).

2.	تحويل العناوين إلى إحداثيات (geocoding) أو العكس.

3.	احتساب المسافات/الوقت بين نقطتين (distance matrix).

4.	عرض معالم (POI) مثل مطاعم ومدارس قريبة آليًا.

لكن في مشروعك:

•	أنت تملك ملفات مثل areas_coordinates.json, poi_landmarks_en_ar.json, area_travel_times_km_min.json

👉 أي كل المعلومات الجغرافية جاهزة ومحلية.

•	الموقع لا يحتاج عرض خريطة تفاعلية من Google Maps، بل فقط نقطة موقع المشروع أو رسم SVG/Leaflet ثابت.

إذًا الـ API ليس له وظيفة جوهرية، بل مجرد بقايا من مكتبة أو سكربت قديم.

⸻

🧹 ما يجب فعله الآن عمليًا

1) احذف أي استدعاء لـ Google Maps API

ابحث في المشروع:

rg -n "maps.googleapis" ./src

rg -n "google.maps" ./src

واحذف أو علّق أي:

<script src=" https://maps.googleapis.com/maps/api/js?key=... "></script>

أو أي كود يستدعي:

new google.maps.Map(...)

⸻

2) استبدال العرض بخريطة محلية (اختياري)

لو أردت خريطة مرئية بسيطة داخل صفحة المشروع:

استخدم Leaflet.js (مفتوحة المصدر، بدون API مفتاح) أو حتى خريطة PNG ثابتة مع نقاط SVG.

import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';

<MapContainer center={[25.2048, 55.2708]} zoom={13} className="h-96 w-full rounded-2xl">

<TileLayer url=" https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png " />

<Marker position={[25.2048, 55.2708]}>

<Popup>Dubai Marina</Popup>

</Marker>

</MapContainer>

⸻

3) نظّف CSP وملفات env

احذف أي مفاتيح أو روابط تخص Google Maps:

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

وأزل المجالات غير اللازمة من CSP:

https://maps.googleapis.com https://maps.gstatic.com

(احذفهما إن لم تعد تستخدم خرائط Google.)

⸻

4) لو احتجت الذكاء الاصطناعي للمواقع الجغرافية

بما أنّ لديك نظام AI داخلي، يمكنك لاحقًا:

•	استخدامه لتصنيف المناطق (قريبة من البحر، قريبة من مول …)،

•	أو حساب المسافات عبر خوارزميات محلية بسيطة (Haversine formula).

كل ذلك دون أي API خارجي.

⸻

✅ النتيجة النهائية

الوظيفة	القرار	السبب

عرض خريطة Google	❌ إزالة	لأنك تملك البيانات المكانية محليًا

جلب إحداثيات	❌ إزالة	الإحداثيات موجودة في JSON

حساب المسافات	❌ إزالة	لديك ملف area_travel_times_km_min.json

خريطة ثابتة بديلة	✅ ممكن	باستخدام Leaflet أو SVG

ربط AI للأماكن	✅ مستقبلي	دون الحاجة إلى Google API

Builder

Thought process

سأفتش عن أي استخدام لـ Google Maps API أو سكربتاته، وأراجع إعدادات next.config.mjs وبيئة .env.local لتنظيف المفاتيح والمجالات غير اللازمة.

Search codebase for 'maps.googleapis|google\.maps'

next.config.mjs 1 - 250

.env.local 1 - 250

Thought

next.config.mjs

+ 0 - 1

Open Diff